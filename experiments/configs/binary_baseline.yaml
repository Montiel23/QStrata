model:
  conv_channels: [32, 64]
  dropout: 0.3
  use_batchnorm: true
  pooling: adaptive_avg

training:
  epochs: 10
  batch_size: 64
  lr: 0.001
  class_weights: true

dataset:
  name: pneumoniamnist
  input_channels: 1
  num_classes: 2
